  <section class="left">
    <form action="/find" method="post" accept-charset="utf-8" class="searchform">
      <input name="term" placeholder="Search for something cute" class="input text" id="term" />
      <button type="submit" class="submit btn">Go!</button>
    </form>

    <% if @name['results'].size < 9 %>
      <p class="morelink">No more <%= @thing %> :(</p>
    <% else %>
      <p class="morelink"><a href="/look/<%= @thing %>/<%= @page += 1 %>">Show me more <%= @thing %>!</a></p>
    <% end %>
  </section>
  <section class="right">
    <% if @name['results'].size == 0 %>
      <p class="novines">No vines for <%= @thing%>! Maybe check out some <a href="/look/pug">pugs</a>?</p>
    <% else %>
      <ul class="puglist">
        <%= erb(:_vidlist, :layout => false) %>
      </ul>
    <% end %>
  </section>
